# Test environment configuration
# This file is for running tests locally
# Uses lightweight models and localhost defaults for testing

# PostgreSQL Test Database
POSTGRES_DB_URL=postgresql://codex_user:test_password@localhost:5432/codex_test
POSTGRES_HOST=localhost
POSTGRES_DB=codex_test
POSTGRES_USER=codex_user
POSTGRES_PASSWORD=test_password

# Ollama LLM Server (lightweight models for faster testing)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:0.5b
OLLAMA_GENERATION_TIMEOUT_SECONDS=30
EMBEDDING_PROVIDER=ollama
EMBEDDING_MODEL=nomic-embed-text
EMBEDDING_BASE_URL=http://localhost:11434
EMBEDDING_TIMEOUT_SECONDS=30

# DuckDB Configuration
DUCKDB_PATH=/tmp/test_memory.duckdb

# Connection Configuration
MAX_DB_CONNECTIONS=10

# dbt Configuration
DBT_PROFILES_DIR=/Users/ladvien/.dbt
DBT_PROJECT_DIR=/Users/ladvien/codex-dreams/biological_memory

# Test Configuration
TEST_DATABASE_URL=postgresql://codex_user:test_password@localhost:5432/codex_test

# Circuit Breaker
MCP_CIRCUIT_BREAKER_ENABLED=false

# NOTE: Timeout configuration moved to OLLAMA_GENERATION_TIMEOUT_SECONDS above

# Biological Parameters
WORKING_MEMORY_CAPACITY=7
STM_DURATION_MINUTES=30
CONSOLIDATION_THRESHOLD=0.6
HEBBIAN_LEARNING_RATE=0.1
FORGETTING_RATE=0.05