# Biological Memory Pipeline - Environment Configuration

# PostgreSQL Source Database
# IMPORTANT: Replace username and password with your actual credentials
# Server is at 192.168.1.104
POSTGRES_DB_URL=postgresql://codex_user:your_password_here@192.168.1.104:5432/codex_db

# Alternative PostgreSQL configuration for DuckDB secrets
# These are used by some SQL scripts that create DuckDB secrets
POSTGRES_HOST=192.168.1.104
POSTGRES_DB=codex_db
POSTGRES_USER=codex_user
POSTGRES_PASSWORD=your_password_here

# Ollama LLM Server
# Server is at 192.168.1.110
OLLAMA_URL=http://192.168.1.110:11434
OLLAMA_MODEL=qwen2.5:0.5b
EMBEDDING_MODEL=nomic-embed-text

# DuckDB Configuration
DUCKDB_PATH=/Users/ladvien/biological_memory/dbs/memory.duckdb

# Connection Configuration
MAX_DB_CONNECTIONS=160

# dbt Configuration
DBT_PROFILES_DIR=/Users/ladvien/.dbt
DBT_PROJECT_DIR=/Users/ladvien/codex-dreams/biological_memory

# Test Configuration
# For testing, use a separate database
TEST_DATABASE_URL=postgresql://codex_user:your_password_here@192.168.1.104:5432/codex_test

# Circuit Breaker (optional)
MCP_CIRCUIT_BREAKER_ENABLED=false

# LLM Timeout Configuration
OLLAMA_TIMEOUT=300

# Biological Parameters (optional - can be set in dbt_project.yml instead)
# WORKING_MEMORY_CAPACITY=7
# STM_DURATION_MINUTES=30
# CONSOLIDATION_THRESHOLD=0.6
# HEBBIAN_LEARNING_RATE=0.1
# FORGETTING_RATE=0.05